Todos
=====

dk: build
    jar bundles for client+shared, server+shared, and combined

dk: provide a Swing-based example
    to prove the multi-toolkit capabilities and have a role-model for KBS/UBS/Siemens

dk: bring grails branch into the master

dk: complete the codec

??: allow a "data" request, i.e. ask server for data that is no PM, aka ReferenceTable

dk: when using @Canonical on Commands, we can possibly remove all toString() methods.
    first check how it looks in the logging, though.

dk: provide an example on how dolphin allows functional testing without UI and
    UI testing without server.
